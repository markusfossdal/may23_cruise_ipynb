# Install/Usage

Build docker image:
```
docker build -t jupyterlab .
```

Start jupyterlab server using docker-compose:
```
docker-compose up -d
```

Open jupyterlab in browser:
```
localhost:8888
```
